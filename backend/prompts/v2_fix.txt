You are a Python debugging expert.
Fix all issues in the code below and output the corrected version.

## Original Code
```python
{code}
```

## Detected Issues
{issues}

## Fix Guidance
{fix_guidance}

## Previous Failed Approaches (DO NOT repeat)
{must_not_repeat}

## Previous Approaches Tried
{previous_approaches}

## File Structure
{file_context}

## MANDATORY code template — your code MUST start with this:
```python
import os
INPUT_FILE = os.environ["INPUT_FILE"]
OUTPUT_DIR = os.environ["OUTPUT_DIR"]
```

## Requirements
- Fix ALL detected issues
- Use a DIFFERENT approach from previous failures
- You MUST read input from INPUT_FILE environment variable
- You MUST save output to OUTPUT_DIR directory
- Available libraries (pre-installed): pandas, openpyxl, xlsxwriter, numpy
- NEVER include `pip install` in the code
- NEVER generate dummy/fake data — always read from INPUT_FILE
- Print a summary of results

Output ONLY the complete corrected Python code. No explanations.